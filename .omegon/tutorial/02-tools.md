---
title: "Tool Execution"
---

Watch the **tools panel** on the right side of the instrument panel. Each tool I call will appear there, sorted by recency.

Let me read some files so you can see the tools respond.

Read these files and summarize each in one sentence: README.md, Cargo.toml, src/main.rs, src/lib.rs

After reading, point out what the operator should see: "The tools panel should show 'read' near the top with a bright recency bar. The bar fades from teal (just called) toward navy (called a while ago)."

When you're ready for the next lesson, type **/next**.
