---
title: "What's Next"
---

🎉 You've completed the Omegon tutorial!

Here's a quick reference for what you've learned:

**Instrument Panel:** Context bar (fill = usage), thinking glitch (density = thinking level), memory strings (→ store, ← recall), tool list (sorted by recency)

**Slash Commands:**
- `/help` — full command list
- `/model` — switch models
- `/think` — adjust thinking level (off/minimal/low/medium/high)
- `/context` — switch context class (scout/squad/battalion)
- `/focus` — toggle instrument panel
- `/tutorial status` — see your tutorial progress
- `/tutorial reset` — restart the tutorial

**Memory:** Facts you store persist across sessions. Use `memory_recall` for targeted search, `memory_store` for important discoveries.

**Design Tree:** Track ideas from seed → exploring → decided → implementing → implemented. Questions capture unknowns, decisions crystallize choices.

Let me clean up the tutorial artifacts now.

Archive the 3 memory facts we stored earlier (they were just for demonstration).
Defer the design node we created: set "tutorial-feature" to deferred status.

You're ready to use Omegon on your own projects. Feel free to keep exploring here or type **/quit** to exit.
